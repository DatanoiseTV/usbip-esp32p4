/*
 * USB/IP Protocol Layer - Stub
 */

#include "usbip_proto.h"
#include "esp_log.h"

static const char *TAG = "usbip_proto";

void usbip_proto_init(void)
{
    ESP_LOGI(TAG, "USB/IP protocol layer initialized (stub)");
}
